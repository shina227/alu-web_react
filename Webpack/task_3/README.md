# Task 3 - Dev servers, modules, and tree shaking

Development-mode Webpack build split into header/body/footer modules with code-split vendor chunks, inline source maps, an auto-generated `index.html`, and a dev server on port 8564. Run `npm install`, then `npm run build` or `npm run start-dev`.
