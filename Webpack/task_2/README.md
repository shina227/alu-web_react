# Task 2 - Adding CSS & Images

Webpack config with CSS and image loaders (plus image optimization) that bundles the dashboard, its `main.css`, and the logo image into `public/`. Run `npm install` then `npm run build`.
