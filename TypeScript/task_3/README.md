# Task 3 - Ambient Namespaces

Uses an ambient declaration file (`crud.d.ts`) to type a plain-JS library (`crud.js`), with a triple-slash directive and shared types from `interface.ts`. Run `npm install`, then `npm run build`.
