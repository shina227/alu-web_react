# TypeScript

ALU/Holberton TypeScript project, learning TypeScript with Webpack, covering interfaces, classes, generics, and DOM rendering. Each `task_x/` folder is a standalone task with its own `package.json`, `tsconfig.json`, and `webpack.config.js`.
