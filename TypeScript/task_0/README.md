# Task 0 - Creating an interface for a student

Defines a `Student` TypeScript interface, builds a `studentsList` of two students, and renders a vanilla-JS `<table>` with each student's first name and location. Run `npm install`, then `npm run build` or `npm run start-dev`.
