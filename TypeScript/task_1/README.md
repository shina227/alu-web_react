# Task 1 - Let's build a Teacher interface

Defines a `Teacher` TypeScript interface with readonly `firstName`/`lastName`, mandatory `fullTimeEmployee`/`location`, optional `yearsOfExperience`, and an index signature allowing any additional attribute. Run `npm install`, then `npm run build`.
